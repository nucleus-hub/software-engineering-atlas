<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ===================================================================
         Software Engineering Atlas — Maven multi-module PARENT (aggregator)
         Java 21 LTS · Spring Boot 3.3.x · JUnit 5
         =================================================================== -->

    <groupId>dev.atlas</groupId>
    <artifactId>software-engineering-atlas</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Software Engineering Atlas</name>
    <description>Master learning repository and hands-on laboratory for full-stack software engineering.</description>

    <!-- Inherit Spring Boot's dependency management (BOM) -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <!-- Aggregated modules. Add new modules here as the lab grows. -->
    <modules>
        <module>backend</module>
        <module>dsa</module>
        <module>pocs</module>
    </modules>

    <!-- Common test tooling available to all modules -->
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
